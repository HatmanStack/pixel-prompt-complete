# Frontend Environment Variables Template
# Copy this file to .env and fill in your values
# Or run the deployment script: ./scripts/deploy.sh [environment]

# Backend API Gateway endpoint
# This is set automatically by deploy.sh or manually after backend deployment
# Format: https://{api-id}.execute-api.{region}.amazonaws.com/Prod
VITE_API_ENDPOINT=https://your-api-id.execute-api.us-west-2.amazonaws.com/Prod

# CloudFront distribution domain for image delivery
# This is set automatically by deploy.sh
# Format: {distribution-id}.cloudfront.net
VITE_CLOUDFRONT_DOMAIN=your-distribution-id.cloudfront.net

# S3 bucket name (for reference, not used by frontend directly)
# Images are served via CloudFront
VITE_S3_BUCKET=pixel-prompt-complete-environment-account-id

# Environment identifier (dev, staging, or prod)
# Used for debugging and analytics
VITE_ENVIRONMENT=dev

# Optional: Enable debug logging in development
# VITE_DEBUG=true

# Optional: Custom API timeout (milliseconds)
# VITE_API_TIMEOUT=30000
